import React from "react";
import { Header } from "../../components";

export const Profile = () => {
  return (
    <div
      style={{
        display: "flex",
        flexDirection: "column",
      }}
    >
      <Header />
      <h1>Profile Page</h1>
    </div>
  );
};
